export const APP_NAME='SUCCI TECH';export const SYNC_STATES=['ONLINE','OFFLINE','SYNCING','SYNC COMPLETE','SYNC FAILED'] as const;export function registerConnectivity(onChange:(online:boolean)=>void){const on=()=>onChange(true),off=()=>onChange(false);addEventListener('online',on);addEventListener('offline',off);return()=>{removeEventListener('online',on);removeEventListener('offline',off)}}